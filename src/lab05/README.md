# Лабораторная работа №5: Функции как аргументы. Стратегии и делегаты.

## Цель работы
Освоить передачу функций как аргументов, использовать `map`, `filter`, `sorted`, реализовать паттерн «Стратегия» и интегрировать функциональный стиль с ООП.

## Реализованные функции и стратегии

### Стратегии сортировки (key-функции)
- `by_title` – по названию
- `by_year` – по году
- `by_price` – по цене
- `by_title_then_year` – по названию, затем по году

### Функции-фильтры
- `is_book`, `is_magazine` – по типу объекта
- `price_less_than(max_price)` – фабрика, создаёт фильтр по цене
- `year_after(min_year)` – фабрика для фильтрации по году

### Функции преобразования (для `map`)
- `apply_discount(percent)` – фабрика, возвращает функцию снижения цены
- `to_string` – преобразование в строку

### Callable-объект (паттерн Стратегия)
- `PriceIncrease` – класс с методом `__call__`, увеличивает цену на заданный процент

### Методы коллекции `AdvancedCollection`
- `sort_by(key_func, reverse)` – сортировка по стратегии
- `filter_by(predicate)` – фильтрация по предикату
- `apply(func)` – применение функции ко всем элементам
- `map(func)` – преобразование элементов

### Цепочки операций
Методы `sort_by`, `filter_by`, `apply` возвращают `self`, что позволяет строить цепочки:
```python
collection.filter_by(cond).sort_by(key).apply(transform)